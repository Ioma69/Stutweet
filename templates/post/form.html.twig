{% extends "base.html.twig" %}

{% block body %}
	{{ form_start(post_form) }}				<!-- {"attr": {"novalidate": "novalidate"}} empeche la validation du formulaire si erreur !! VALIDATION ne passe plus coté client !! -->
	{{ form_rest(post_form) }}
	<input class="btn btn-primary" type="submit" value="Publier">
{% endblock %}